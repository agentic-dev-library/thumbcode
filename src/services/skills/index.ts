/**
 * Agent Skills
 *
 * Composable skill modules that extend agent capabilities
 * with domain-specific context, tools, and behavior.
 */

export { FrontendSkill } from './FrontendSkill';
export type { AgentSkill, ContextTier, SkillToolDefinition, SkillToolParameter } from './types';
