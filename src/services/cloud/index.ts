/**
 * Cloud Save Services
 *
 * Local and cloud document storage (Google Drive, OneDrive — future).
 */

export type { CloudProvider, CloudSaveResult } from './CloudSaveService';
export { CloudSaveService } from './CloudSaveService';
